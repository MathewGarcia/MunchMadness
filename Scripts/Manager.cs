using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Manager : MonoBehaviour
{
    // Each postion in orderval number represents a certain ingredient
    // When you click on it, correspoding increasing by number of times you click on it
    // So right now there is two number because the test right now is a cookie and a cupcake 

    public static int orderVal = 11;
    public static int plateVal = 00;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
